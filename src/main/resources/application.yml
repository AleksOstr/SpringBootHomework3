application:
  max-allowed-issues: 1

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: on
  mvc:
    hiddenmethod:
      filter:
        enabled: true

